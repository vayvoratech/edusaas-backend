PORT=5000
JWT_SECRET=replace-this-with-a-long-random-secret
JWT_EXPIRES_IN=7d

# Set USE_DB=true to use Postgres (Prisma); false uses in-memory store.
USE_DB=true

# PostgreSQL connection (schema 'education')
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/edu_saas_db?schema=education
